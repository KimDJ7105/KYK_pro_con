소켓이 필요
소켓에 연결하고자 하는 서버의 IP와 포트번호를 입력
연결
===============
일단 서버는

보내기 받기만 있을 뿐이지
뭘 보내고 뭘 받을지는 전적으로 우리의 몫이다.
(예로 오브젝트의 움직임에 대한 행렬만 옮길지, 아니면 오브젝트를 다 움직인 데이터를 옮길지)

서버전용 소켓
클라이언트 전용 소켓(여기에 IP와 포트번호가 존재)

서버 소켓에서 Assept(문열어줌)

클라이언트에서 Connect를 시도해서
서버가 Connect된 클라이언트의 IP와 포트번호를 받아둔다.

Blocking이란게 있는데
Send라는 과정에서
통신버퍼와 수신버퍼중
클라가 보낼 내용을 통신버퍼에 복사를 하는 과정이 있다.
이때 다음거로 안넘어가게 하는 것이 Blocking이다.

